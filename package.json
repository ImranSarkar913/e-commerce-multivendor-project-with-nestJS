{
  "scripts": {
    "migration:run": "npm --prefix ecommerce run migration:run",
    "migration:revert": "npm --prefix ecommerce run migration:revert",
    "migration:generate": "npm --prefix ecommerce run migration:generate",
    "migration:create": "npm --prefix ecommerce run migration:create",
    "typecheck:root": "tsc -p tsconfig.json"
  },
  "dependencies": {
    "@nestjs/typeorm": "^11.0.1",
    "dotenv": "^17.4.2",
    "typeorm": "^0.3.28"
  },
  "devDependencies": {
    "@types/node": "^22.15.3",
    "typescript": "^5.7.3"
  }
}
